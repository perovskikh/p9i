Thank you for installing p9i!

Your release is named {{ .Release.Name }}.

To get further information, run:

  helm status {{ .Release.Name }}
  kubectl get pods -n {{ .Release.Namespace }} -l app={{ include "p9i.fullname" . }}

🚀 Access Points:

  MCP Server: {{ include "p9i.fullname" . }}
  Port: 8000
  Namespace: {{ .Release.Namespace }}

🔑 Configuration:

  Replicas: {{ .Values.replicaCount }}
  Image: {{ .Values.image.repository }}:{{ .Values.image.tag }}

🐘 Database:

{{- if .Values.postgresql.enabled }}
  PostgreSQL: {{ include "p9i.fullname" . }}-postgresql
{{- end }}

🧰 Cache:

{{- if .Values.redis.enabled }}
  Redis: {{ include "p9i.fullname" . }}-redis-master
{{- end }}

🔐 Security:

  Secrets: {{ include "p9i.fullname" . }}-secrets

📚 Troubleshooting:

1. Check pod status:
   kubectl get pods -n {{ .Release.Namespace }} -l app={{ include "p9i.fullname" . }}

2. View logs:
   kubectl logs -n {{ .Release.Namespace }} deployment/{{ .Release.Name }} -f --tail=100

3. Check events:
   kubectl get events -n {{ .Release.Namespace }} --sort-by='.lastTimestamp'

📖 Documentation:

  Full docs: ./README.md

💡 Tips:

- Use `helm upgrade --install` for idempotent installation
- Check MicroK8s addon status: `microk8s addons list`
- Backup PostgreSQL data before major upgrades

Happy prompting! 🚀