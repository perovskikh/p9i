# Caddyfile for AI Prompt System
# HTTP only - HTTPS requires proper DNS configuration

{
    admin off
}

# MCP Server via HTTP (port 80)
# For HTTPS, configure DNS A record first
mcp.coderweb.ru, ai.coderweb.ru, localhost {
    reverse_proxy mcp-server:8000
    encode gzip
}
