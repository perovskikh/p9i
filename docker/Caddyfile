# Caddyfile for AI Prompt System
# Automatic HTTPS with Let's Encrypt

{
    email admin@coderweb.ru
    # Auto HTTP → HTTPS
    http_port 80
    https_port 443
}

# Production domain (after DNS is configured)
# mcp.coderweb.ru {
#     reverse_proxy mcp-server:8000
#     encode gzip
#     header { Strict-Transport-Security "max-age=31536000" }
# }

# Development - HTTP only (no TLS until DNS configured)
http://coderweb.ru, http://localhost, http://mcp.localhost {
    reverse_proxy mcp-server:8000
    encode gzip
}
